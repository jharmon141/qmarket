import Ember from 'ember';

export default Ember.Route.extend({
    actions: {
        logout() {
            // write code that calls /logout api for user and navigate to /login page
        }
    }
});
